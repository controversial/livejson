from setuptools import setup

setup(
    name="livejson",
    version="0.1",
    description=("An interface to transparantly bind Python objects to JSON "
                 "files so that all changes made to the object are reflected "
                 "in the JSON file"),
    author="Luke Taylor",
    author_email="luke@deentaylor.com",
    url="http://luke.deentaylor.com/",
)
